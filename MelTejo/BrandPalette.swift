import SwiftUI

enum BrandPalette {
    static let background = Color(red: 0.06, green: 0.05, blue: 0.03)
    static let panel = Color(red: 0.12, green: 0.10, blue: 0.06)
    static let panelSoft = Color(red: 0.22, green: 0.18, blue: 0.10)
    static let ink = Color(red: 0.03, green: 0.02, blue: 0.01)
    static let accent = Color(red: 0.96, green: 0.76, blue: 0.16)
    static let accentDeep = Color(red: 0.67, green: 0.49, blue: 0.06)
    static let glow = Color(red: 1.00, green: 0.92, blue: 0.65)
    static let success = Color(red: 0.93, green: 0.79, blue: 0.28)
    static let warning = Color(red: 0.97, green: 0.88, blue: 0.58)
    static let text = Color.white
    static let secondaryText = Color.white.opacity(0.72)
    static let mutedText = Color.white.opacity(0.48)
}
